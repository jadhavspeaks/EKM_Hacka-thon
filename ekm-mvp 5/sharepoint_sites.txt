# SharePoint Sites - EKM Knowledge Crawler
# Host: share.nam.nsroot.net | NTLM domain: nam
#
# CONFIRMED WORKING (from probe logs):
#   https://share.nam.nsroot.net/citi.net  -> 500 pages, Libraries: Citihet-Documents, Documents, TEST
#
# Run probe to discover more: python utils/sp_adfs_probe.py
#
# SITE TYPES:
#   /sites/<name>  -> Wiki mode  - crawls Site Pages + Document Libraries
#   /teams/<name>  -> Docs mode  - crawls Document Libraries only
#   /<name>        -> Direct     - crawls as-is (like /citi.net below)
#

https://share.nam.nsroot.net/citi.net
